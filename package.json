{
  "name": "codeshelf",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed for static + serverless functions'",
    "start": "vercel dev"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": "22.x"
  }
}
